// 4. SPLICE
    // splice (indexAwal, MauDihapusBerapa?, elemenBaru1, elemenBaru2, ... )
    // arr.splice(2, 2, 'fana', 'oteng');
    // console.log(arr.join(' - '));
    
    // 5. SLICE
    var arr = ["lukman", "sifaul", "nico", "farel"];
    var arr2 = arr.slice(1,3);
    console.log(arr2.join(' - '));
    console.log(arr.join(' - '));